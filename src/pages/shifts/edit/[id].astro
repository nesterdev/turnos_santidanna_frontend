---
export const prerender = false; // desactiva prerender para SSR

import DashboardLayout from "../../../components/layouts/DashboardLayout.astro";
import ShiftsEditForm from "../../../components/shifts/ShiftsEditForm";

const { id } = Astro.params;
---

<DashboardLayout title={`Editar Empleado #${id}`}>
  <ShiftsEditForm client:load id={id} />
</DashboardLayout>
